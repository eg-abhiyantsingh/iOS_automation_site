[
  {
    "match": "AssetEngineer",
    "match_text": ["add custom", "match panel", "match header", "custom entry", "custom sheet", "manufacturer chip"],
    "exclude": true,
    "reason": "INVALID-AUTOMATION (user-verified live 2026-08-11): v1.55 gates the SKM match panel behind the Engineering 'Use library' toggle, which defaults OFF; the automation never enabled it, so 'Add Custom'/match-panel elements were genuinely absent for the tests but NOT for a user who flips the toggle. Fixed via AssetEngineerPage.ensureUseLibraryEnabled(); remove this entry once the family is green on CI."
  },
  {
    "match": "PhotoWalkthrough",
    "severity": "High",
    "priority": "High",
    "note": "Known product bug CAM-CRASH-01: tapping Camera crashes the app on simulators (UIImagePickerController without an isSourceTypeAvailable check)."
  },
  {
    "match": "TC_WT_X_NEG",
    "severity": "High",
    "priority": "Medium",
    "note": "Known product bug WT-NEG-01: the backend accepts a work-order create with a non-existent work_type_id instead of rejecting it."
  },
  {
    "match": "CONN_VAL",
    "severity": "Medium",
    "priority": "Medium",
    "note": "Related to product bug CONN-VAL-01: creating a connection without a target leaves a 'Not Assigned' debris row."
  }
]
