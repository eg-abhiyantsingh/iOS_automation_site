name: Automation Test - Parallel

# ============================================================
# DEVELOPER REPO - FULL PARALLEL TEST SUITE
# ============================================================
# Builds iOS app from selected branch, then runs all test
# modules in parallel across 9 separate macOS runners.
#
# Architecture:
#   Job 0:  build-app          → Build iOS app from source (1 runner)
#   Job 1:  auth-tests         → Authentication (38 tests)
#   Job 2:  site-tests         → Site Selection (52 tests)
#   Job 3:  phase1-tests       → Asset Phase 1 (139 tests)
#   Job 4:  phase2-tests       → Asset Phase 2 (159 tests)
#   Job 5:  phase3-tests       → Asset Phase 3 (169 tests)
#   Job 6:  phase4-tests       → Asset Phase 4 (141 tests)
#   Job 7:  phase5-tests       → Asset Phase 5 (45 tests)
#   Job 8:  location-tests     → Locations (82 tests)
#   Job 9:  connections-tests  → Connections (94 tests)
#   Job 10: summary            → Aggregate results + email
#
# Total: ~921 tests, ~2.5 hr wall clock (parallel)
#
# Requirements:
#   Secrets: QA_REPO_TOKEN, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY,
#            EMAIL_USERNAME, EMAIL_PASSWORD, EMAIL_TO
# ============================================================

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to build and test'
        required: true
        default: 'release/qa'
        type: choice
        options:
          - main
          - release/dev
          - release/qa
          - release/stage
          - release/prod
      job_selection:
        description: 'Which jobs to run'
        required: false
        default: 'all'
        type: choice
        options:
          - 'all'
          - 'auth-only'
          - 'site-only'
          - 'phase1-only'
          - 'phase2-only'
          - 'phase3-only'
          - 'phase4-only'
          - 'phase5-only'
          - 'location-only'
          - 'connections-only'

# ============================================================
# JOB 0: Build iOS App from Source
# ============================================================
jobs:
  build-app:
    runs-on: macos-15
    timeout-minutes: 60

    steps:
      - name: Checkout iOS App Repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}
          path: ios-app

      - name: Checkout QA Automation Repository
        uses: actions/checkout@v4
        with:
          repository: eg-abhiyantsingh/iOS_automation_site
          token: ${{ secrets.QA_REPO_TOKEN }}
          path: qa-automation

      - name: Set Build Configuration
        run: |
          BRANCH="${{ github.event.inputs.branch }}"

          case "$BRANCH" in
            release/dev)     SCHEME="Egalvanic PZ-Dev" ;;
            release/qa)      SCHEME="Egalvanic PZ-QA" ;;
            release/stage*)  SCHEME="Egalvanic PZ-Staging" ;;
            release/prod|main) SCHEME="Egalvanic PZ" ;;
            *)               SCHEME="Egalvanic PZ-QA" ;;
          esac

          XCODEPROJ=$(find ios-app -name "*.xcodeproj" -type d | head -1)

          echo "BUILD_SCHEME=$SCHEME" >> $GITHUB_ENV
          echo "XCODEPROJ_PATH=$XCODEPROJ" >> $GITHUB_ENV

          echo "📱 Branch: $BRANCH | Scheme: $SCHEME"

      - name: Boot Simulator for Build
        run: |
          UDID=""
          for VERSION in 18.5 18.4 18.3 18.2 18.1 18.0; do
            for MODEL in "iPhone 16 Pro" "iPhone 15 Pro" "iPhone"; do
              UDID=$(xcrun simctl list devices available | grep -A 100 "iOS $VERSION" | grep "$MODEL" | head -1 | awk -F '[()]' '{print $2}')
              [ -n "$UDID" ] && break 2
            done
          done

          if [ -z "$UDID" ]; then
            echo "❌ No iPhone simulator found!"
            xcrun simctl list devices available
            exit 1
          fi

          echo "BUILD_UDID=$UDID" >> $GITHUB_ENV
          echo "📱 Build simulator: $UDID"

          xcrun simctl shutdown "$UDID" 2>/dev/null || true
          sleep 3
          xcrun simctl boot "$UDID" 2>/dev/null || true

          TIMEOUT=120
          ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            if xcrun simctl list devices | grep "$UDID" | grep -q "(Booted)"; then
              echo "✅ Booted after ${ELAPSED}s"
              break
            fi
            sleep 5
            ELAPSED=$((ELAPSED + 5))
          done

          if [ $ELAPSED -ge $TIMEOUT ]; then echo "❌ Boot timeout"; exit 1; fi

      - name: Resolve Swift Package Dependencies
        run: |
          cd ios-app
          PROJECT_FILE=$(basename "$XCODEPROJ_PATH")
          xcodebuild -resolvePackageDependencies \
            -project "$PROJECT_FILE" \
            -scheme "${{ env.BUILD_SCHEME }}" \
            -clonedSourcePackagesDirPath ../spm-cache \
            -quiet 2>&1 | tail -3
          echo "✅ Dependencies resolved"

      - name: Build iOS App for Simulator
        run: |
          cd ios-app
          PROJECT_FILE=$(basename "$XCODEPROJ_PATH")

          echo "🔨 Building: ${{ env.BUILD_SCHEME }} from ${{ github.event.inputs.branch }}"

          xcodebuild build \
            -project "$PROJECT_FILE" \
            -scheme "${{ env.BUILD_SCHEME }}" \
            -destination "platform=iOS Simulator,id=${{ env.BUILD_UDID }}" \
            -derivedDataPath build \
            -clonedSourcePackagesDirPath ../spm-cache \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_REQUIRED=NO \
            -quiet 2>&1 | tail -5

          APP_PATH=$(find build -name "*.app" -type d | head -1)

          if [ -z "$APP_PATH" ]; then
            echo "❌ Build failed! No .app found"
            find build -type d | head -20
            exit 1
          fi

          echo "✅ Built: $(basename "$APP_PATH")"

          # Copy built app to QA automation directory
          mkdir -p ../qa-automation/apps
          cp -R "$APP_PATH" "../qa-automation/apps/"

      - name: Verify App Configuration
        run: |
          APP_FILE=$(ls qa-automation/apps/ | grep ".app" | head -1)
          APP="qa-automation/apps/$APP_FILE"

          if [ -f "$APP/Info.plist" ]; then
            echo "─── Built App Config ───"
            echo "  Bundle ID:    $(/usr/libexec/PlistBuddy -c 'Print :CFBundleIdentifier' "$APP/Info.plist" 2>/dev/null || echo 'N/A')"
            echo "  API URL:      $(/usr/libexec/PlistBuddy -c 'Print :API_BASE_URL' "$APP/Info.plist" 2>/dev/null || echo 'N/A')"
            echo "  Environment:  $(/usr/libexec/PlistBuddy -c 'Print :ENVIRONMENT_NAME' "$APP/Info.plist" 2>/dev/null || echo 'N/A')"
            echo "  Version:      $(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' "$APP/Info.plist" 2>/dev/null || echo 'N/A')"
          fi

      - name: Upload Built App
        uses: actions/upload-artifact@v4
        with:
          name: built-app
          path: qa-automation/apps/
          retention-days: 1

      - name: Upload QA Automation Code
        uses: actions/upload-artifact@v4
        with:
          name: qa-code
          path: |
            qa-automation/src/
            qa-automation/pom.xml
            qa-automation/testng.xml
            qa-automation/testng-smoke.xml
            qa-automation/baselines/
          retention-days: 1

      - name: Shutdown Build Simulator
        if: always()
        run: xcrun simctl shutdown "${{ env.BUILD_UDID }}" 2>/dev/null || true

  # ============================================================
  # JOB 1: Auth Tests (38 tests, ~30 min)
  # ============================================================
  auth-tests:
    needs: [build-app]
    if: github.event.inputs.job_selection == 'all' || github.event.inputs.job_selection == 'auth-only'
    runs-on: macos-15
    timeout-minutes: 420

    steps:
      - name: Download Built App
        uses: actions/download-artifact@v4
        with:
          name: built-app
          path: apps/

      - name: Download QA Automation Code
        uses: actions/download-artifact@v4
        with:
          name: qa-code
          path: qa-automation/

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Appium
        run: |
          npm install -g appium@latest 2>&1 | tail -1
          appium driver install xcuitest 2>&1 | tail -1
          echo "✅ Appium $(appium --version)"

      - name: Boot Simulator
        run: |
          UDID=""
          for VERSION in 18.5 18.4 18.3 18.2 18.1 18.0; do
            for MODEL in "iPhone 16 Pro" "iPhone 15 Pro" "iPhone"; do
              UDID=$(xcrun simctl list devices available | grep -A 100 "iOS $VERSION" | grep "$MODEL" | head -1 | awk -F '[()]' '{print $2}')
              [ -n "$UDID" ] && break 2
            done
          done

          if [ -z "$UDID" ]; then
            echo "❌ No iPhone simulator found!"
            xcrun simctl list devices available
            exit 1
          fi

          DEVICE_NAME=$(xcrun simctl list devices available | grep "$UDID" | sed 's/(.*//' | xargs)
          PLATFORM_VERSION=$(xcrun simctl list devices available | grep -B 20 "$UDID" | grep "iOS" | tail -1 | sed 's/-- iOS //' | sed 's/ --.*//')
          [ -z "$PLATFORM_VERSION" ] && PLATFORM_VERSION="18.5"

          echo "DEVICE_NAME=$DEVICE_NAME" >> $GITHUB_ENV
          echo "SIMULATOR_UDID=$UDID" >> $GITHUB_ENV
          echo "PLATFORM_VERSION=$PLATFORM_VERSION" >> $GITHUB_ENV
          echo "📱 $DEVICE_NAME | iOS $PLATFORM_VERSION | $UDID"

          xcrun simctl shutdown "$UDID" 2>/dev/null || true
          sleep 3
          xcrun simctl boot "$UDID" 2>/dev/null || true

          TIMEOUT=120
          ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            if xcrun simctl list devices | grep "$UDID" | grep -q "(Booted)"; then
              echo "✅ Booted after ${ELAPSED}s"
              break
            fi
            sleep 5
            ELAPSED=$((ELAPSED + 5))
          done

          if [ $ELAPSED -ge $TIMEOUT ]; then echo "❌ Boot timeout"; exit 1; fi
          sleep 45
          echo "✅ Simulator ready"

      - name: Install App on Simulator
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          xcrun simctl install "${{ env.SIMULATOR_UDID }}" "apps/$APP_FILE"
          echo "✅ App installed: $APP_FILE"

      - name: Pre-build WebDriverAgent
        run: |
          WDA_PATH=$(find ~/.appium -name "WebDriverAgent.xcodeproj" -type f 2>/dev/null | head -1 | xargs dirname)
          if [ -n "$WDA_PATH" ]; then
            cd "$WDA_PATH"
            xcodebuild build-for-testing \
              -project WebDriverAgent.xcodeproj \
              -scheme WebDriverAgentRunner \
              -destination "platform=iOS Simulator,id=${{ env.SIMULATOR_UDID }}" \
              -derivedDataPath /tmp/wda-build \
              -quiet 2>&1 | tail -3
            echo "✅ WDA pre-built"
          fi

      - name: Start Appium Server
        run: |
          appium --relaxed-security --log-level error > appium.log 2>&1 &
          for i in {1..60}; do
            if curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4723/status 2>/dev/null | grep -q "200"; then
              echo "✅ Appium ready"
              break
            fi
            sleep 2
          done
          sleep 10

      - name: Warm-up WDA Session
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          FULL_APP_PATH="${{ github.workspace }}/apps/$APP_FILE"

          for attempt in 1 2 3; do
            echo "WDA warm-up attempt $attempt/3..."
            SESSION_RESPONSE=$(curl -s -X POST http://127.0.0.1:4723/session \
              -H "Content-Type: application/json" \
              -d '{
                "capabilities": {
                  "alwaysMatch": {
                    "platformName": "iOS",
                    "appium:automationName": "XCUITest",
                    "appium:deviceName": "'"${{ env.DEVICE_NAME }}"'",
                    "appium:udid": "'"${{ env.SIMULATOR_UDID }}"'",
                    "appium:platformVersion": "'"${{ env.PLATFORM_VERSION }}"'",
                    "appium:app": "'"$FULL_APP_PATH"'",
                    "appium:wdaLaunchTimeout": 600000,
                    "appium:wdaConnectionTimeout": 300000,
                    "appium:useNewWDA": false,
                    "appium:noReset": false
                  }
                }
              }' --max-time 600 2>&1) || true

            SESSION_ID=$(echo "$SESSION_RESPONSE" | grep -o '"sessionId":"[^"]*"' | cut -d'"' -f4 || true)

            if [ -n "$SESSION_ID" ]; then
              echo "✅ WDA session OK"
              sleep 10
              curl -s -X DELETE "http://127.0.0.1:4723/session/$SESSION_ID" || true
              sleep 5
              echo "✅ WDA warm-up complete"
              break
            else
              [ $attempt -lt 3 ] && sleep 30
            fi
          done

      - name: Run Auth Tests
        continue-on-error: true
        run: |
          cd qa-automation
          APP_FILE=$(ls ../apps/ | grep ".app" | head -1)
          APP_PATH="${{ github.workspace }}/apps/$APP_FILE"

          mvn test -B -q \
            -DsuiteXmlFile=src/test/resources/parallel/testng-auth.xml \
            -DDEVICE_NAME="${{ env.DEVICE_NAME }}" \
            -DPLATFORM_VERSION="${{ env.PLATFORM_VERSION }}" \
            -DSIMULATOR_UDID="${{ env.SIMULATOR_UDID }}" \
            -DAPP_PATH="$APP_PATH"

      - name: Upload Auth Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: auth-report
          path: |
            qa-automation/target/surefire-reports/
            qa-automation/reports/
            qa-automation/screenshots/
          retention-days: 7

      - name: Upload Appium Log (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: appium-log-auth-${{ github.run_number }}
          path: appium.log
          retention-days: 7

      - name: Shutdown Simulator
        if: always()
        run: xcrun simctl shutdown "${{ env.SIMULATOR_UDID }}" 2>/dev/null || true

  # ============================================================
  # JOB 2: Site Tests (52 tests, ~1 hr)
  # ============================================================
  site-tests:
    needs: [build-app]
    if: github.event.inputs.job_selection == 'all' || github.event.inputs.job_selection == 'site-only'
    runs-on: macos-15
    timeout-minutes: 420

    steps:
      - name: Download Built App
        uses: actions/download-artifact@v4
        with:
          name: built-app
          path: apps/

      - name: Download QA Automation Code
        uses: actions/download-artifact@v4
        with:
          name: qa-code
          path: qa-automation/

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Appium
        run: |
          npm install -g appium@latest 2>&1 | tail -1
          appium driver install xcuitest 2>&1 | tail -1
          echo "✅ Appium $(appium --version)"

      - name: Boot Simulator
        run: |
          UDID=""
          for VERSION in 18.5 18.4 18.3 18.2 18.1 18.0; do
            for MODEL in "iPhone 16 Pro" "iPhone 15 Pro" "iPhone"; do
              UDID=$(xcrun simctl list devices available | grep -A 100 "iOS $VERSION" | grep "$MODEL" | head -1 | awk -F '[()]' '{print $2}')
              [ -n "$UDID" ] && break 2
            done
          done

          if [ -z "$UDID" ]; then
            echo "❌ No iPhone simulator found!"
            xcrun simctl list devices available
            exit 1
          fi

          DEVICE_NAME=$(xcrun simctl list devices available | grep "$UDID" | sed 's/(.*//' | xargs)
          PLATFORM_VERSION=$(xcrun simctl list devices available | grep -B 20 "$UDID" | grep "iOS" | tail -1 | sed 's/-- iOS //' | sed 's/ --.*//')
          [ -z "$PLATFORM_VERSION" ] && PLATFORM_VERSION="18.5"

          echo "DEVICE_NAME=$DEVICE_NAME" >> $GITHUB_ENV
          echo "SIMULATOR_UDID=$UDID" >> $GITHUB_ENV
          echo "PLATFORM_VERSION=$PLATFORM_VERSION" >> $GITHUB_ENV
          echo "📱 $DEVICE_NAME | iOS $PLATFORM_VERSION | $UDID"

          xcrun simctl shutdown "$UDID" 2>/dev/null || true
          sleep 3
          xcrun simctl boot "$UDID" 2>/dev/null || true

          TIMEOUT=120
          ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            if xcrun simctl list devices | grep "$UDID" | grep -q "(Booted)"; then
              echo "✅ Booted after ${ELAPSED}s"
              break
            fi
            sleep 5
            ELAPSED=$((ELAPSED + 5))
          done

          if [ $ELAPSED -ge $TIMEOUT ]; then echo "❌ Boot timeout"; exit 1; fi
          sleep 45
          echo "✅ Simulator ready"

      - name: Install App on Simulator
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          xcrun simctl install "${{ env.SIMULATOR_UDID }}" "apps/$APP_FILE"
          echo "✅ App installed: $APP_FILE"

      - name: Pre-build WebDriverAgent
        run: |
          WDA_PATH=$(find ~/.appium -name "WebDriverAgent.xcodeproj" -type f 2>/dev/null | head -1 | xargs dirname)
          if [ -n "$WDA_PATH" ]; then
            cd "$WDA_PATH"
            xcodebuild build-for-testing \
              -project WebDriverAgent.xcodeproj \
              -scheme WebDriverAgentRunner \
              -destination "platform=iOS Simulator,id=${{ env.SIMULATOR_UDID }}" \
              -derivedDataPath /tmp/wda-build \
              -quiet 2>&1 | tail -3
            echo "✅ WDA pre-built"
          fi

      - name: Start Appium Server
        run: |
          appium --relaxed-security --log-level error > appium.log 2>&1 &
          for i in {1..60}; do
            if curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4723/status 2>/dev/null | grep -q "200"; then
              echo "✅ Appium ready"
              break
            fi
            sleep 2
          done
          sleep 10

      - name: Warm-up WDA Session
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          FULL_APP_PATH="${{ github.workspace }}/apps/$APP_FILE"

          for attempt in 1 2 3; do
            echo "WDA warm-up attempt $attempt/3..."
            SESSION_RESPONSE=$(curl -s -X POST http://127.0.0.1:4723/session \
              -H "Content-Type: application/json" \
              -d '{
                "capabilities": {
                  "alwaysMatch": {
                    "platformName": "iOS",
                    "appium:automationName": "XCUITest",
                    "appium:deviceName": "'"${{ env.DEVICE_NAME }}"'",
                    "appium:udid": "'"${{ env.SIMULATOR_UDID }}"'",
                    "appium:platformVersion": "'"${{ env.PLATFORM_VERSION }}"'",
                    "appium:app": "'"$FULL_APP_PATH"'",
                    "appium:wdaLaunchTimeout": 600000,
                    "appium:wdaConnectionTimeout": 300000,
                    "appium:useNewWDA": false,
                    "appium:noReset": false
                  }
                }
              }' --max-time 600 2>&1) || true

            SESSION_ID=$(echo "$SESSION_RESPONSE" | grep -o '"sessionId":"[^"]*"' | cut -d'"' -f4 || true)

            if [ -n "$SESSION_ID" ]; then
              echo "✅ WDA session OK"
              sleep 10
              curl -s -X DELETE "http://127.0.0.1:4723/session/$SESSION_ID" || true
              sleep 5
              echo "✅ WDA warm-up complete"
              break
            else
              [ $attempt -lt 3 ] && sleep 30
            fi
          done

      - name: Run Site Tests
        continue-on-error: true
        run: |
          cd qa-automation
          APP_FILE=$(ls ../apps/ | grep ".app" | head -1)
          APP_PATH="${{ github.workspace }}/apps/$APP_FILE"

          mvn test -B -q \
            -DsuiteXmlFile=src/test/resources/parallel/testng-site.xml \
            -DDEVICE_NAME="${{ env.DEVICE_NAME }}" \
            -DPLATFORM_VERSION="${{ env.PLATFORM_VERSION }}" \
            -DSIMULATOR_UDID="${{ env.SIMULATOR_UDID }}" \
            -DAPP_PATH="$APP_PATH"

      - name: Upload Site Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: site-report
          path: |
            qa-automation/target/surefire-reports/
            qa-automation/reports/
            qa-automation/screenshots/
          retention-days: 7

      - name: Upload Appium Log (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: appium-log-site-${{ github.run_number }}
          path: appium.log
          retention-days: 7

      - name: Shutdown Simulator
        if: always()
        run: xcrun simctl shutdown "${{ env.SIMULATOR_UDID }}" 2>/dev/null || true


  # ============================================================
  # JOB 3: Asset Phase 1 Tests (139 tests, ~2 hr)
  # ============================================================
  phase1-tests:
    needs: [build-app]
    if: github.event.inputs.job_selection == 'all' || github.event.inputs.job_selection == 'phase1-only'
    runs-on: macos-15
    timeout-minutes: 420

    steps:
      - name: Download Built App
        uses: actions/download-artifact@v4
        with:
          name: built-app
          path: apps/

      - name: Download QA Automation Code
        uses: actions/download-artifact@v4
        with:
          name: qa-code
          path: qa-automation/

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Appium
        run: |
          npm install -g appium@latest 2>&1 | tail -1
          appium driver install xcuitest 2>&1 | tail -1
          echo "✅ Appium $(appium --version)"

      - name: Boot Simulator
        run: |
          UDID=""
          for VERSION in 18.5 18.4 18.3 18.2 18.1 18.0; do
            for MODEL in "iPhone 16 Pro" "iPhone 15 Pro" "iPhone"; do
              UDID=$(xcrun simctl list devices available | grep -A 100 "iOS $VERSION" | grep "$MODEL" | head -1 | awk -F '[()]' '{print $2}')
              [ -n "$UDID" ] && break 2
            done
          done
          [ -z "$UDID" ] && echo "❌ No simulator found!" && exit 1

          DEVICE_NAME=$(xcrun simctl list devices available | grep "$UDID" | sed 's/(.*//' | xargs)
          PLATFORM_VERSION=$(xcrun simctl list devices available | grep -B 20 "$UDID" | grep "iOS" | tail -1 | sed 's/-- iOS //' | sed 's/ --.*//')
          [ -z "$PLATFORM_VERSION" ] && PLATFORM_VERSION="18.5"

          echo "DEVICE_NAME=$DEVICE_NAME" >> $GITHUB_ENV
          echo "SIMULATOR_UDID=$UDID" >> $GITHUB_ENV
          echo "PLATFORM_VERSION=$PLATFORM_VERSION" >> $GITHUB_ENV
          echo "📱 $DEVICE_NAME | iOS $PLATFORM_VERSION | $UDID"

          xcrun simctl shutdown "$UDID" 2>/dev/null || true
          sleep 3
          xcrun simctl boot "$UDID" 2>/dev/null || true

          TIMEOUT=120; ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            if xcrun simctl list devices | grep "$UDID" | grep -q "(Booted)"; then
              echo "✅ Booted after ${ELAPSED}s"; break
            fi
            sleep 5; ELAPSED=$((ELAPSED + 5))
          done
          if [ $ELAPSED -ge $TIMEOUT ]; then echo "❌ Boot timeout"; exit 1; fi
          sleep 45
          echo "✅ Simulator ready"

      - name: Install App on Simulator
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          xcrun simctl install "${{ env.SIMULATOR_UDID }}" "apps/$APP_FILE"
          echo "✅ App installed"

      - name: Pre-build WebDriverAgent
        run: |
          WDA_PATH=$(find ~/.appium -name "WebDriverAgent.xcodeproj" -type f 2>/dev/null | head -1 | xargs dirname)
          if [ -n "$WDA_PATH" ]; then
            cd "$WDA_PATH"
            xcodebuild build-for-testing -project WebDriverAgent.xcodeproj -scheme WebDriverAgentRunner \
              -destination "platform=iOS Simulator,id=${{ env.SIMULATOR_UDID }}" \
              -derivedDataPath /tmp/wda-build -quiet 2>&1 | tail -3
            echo "✅ WDA pre-built"
          fi

      - name: Start Appium Server
        run: |
          appium --relaxed-security --log-level error > appium.log 2>&1 &
          for i in {1..60}; do
            if curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4723/status 2>/dev/null | grep -q "200"; then
              echo "✅ Appium ready"; break
            fi
            sleep 2
          done
          sleep 10

      - name: Warm-up WDA Session
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          FULL_APP_PATH="${{ github.workspace }}/apps/$APP_FILE"

          for attempt in 1 2 3; do
            SESSION_RESPONSE=$(curl -s -X POST http://127.0.0.1:4723/session \
              -H "Content-Type: application/json" \
              -d '{"capabilities":{"alwaysMatch":{"platformName":"iOS","appium:automationName":"XCUITest","appium:deviceName":"'"${{ env.DEVICE_NAME }}"'","appium:udid":"'"${{ env.SIMULATOR_UDID }}"'","appium:platformVersion":"'"${{ env.PLATFORM_VERSION }}"'","appium:app":"'"$FULL_APP_PATH"'","appium:wdaLaunchTimeout":600000,"appium:wdaConnectionTimeout":300000,"appium:useNewWDA":false,"appium:noReset":false}}}' \
              --max-time 600 2>&1) || true
            SESSION_ID=$(echo "$SESSION_RESPONSE" | grep -o '"sessionId":"[^"]*"' | cut -d'"' -f4 || true)
            if [ -n "$SESSION_ID" ]; then
              echo "✅ WDA warm-up OK"; sleep 10
              curl -s -X DELETE "http://127.0.0.1:4723/session/$SESSION_ID" || true
              sleep 5; break
            else
              [ $attempt -lt 3 ] && sleep 30
            fi
          done

      - name: Run Phase 1 Tests
        continue-on-error: true
        run: |
          cd qa-automation
          APP_FILE=$(ls ../apps/ | grep ".app" | head -1)
          mvn test -B -q \
            -DsuiteXmlFile=src/test/resources/parallel/testng-phase1.xml \
            -DDEVICE_NAME="${{ env.DEVICE_NAME }}" \
            -DPLATFORM_VERSION="${{ env.PLATFORM_VERSION }}" \
            -DSIMULATOR_UDID="${{ env.SIMULATOR_UDID }}" \
            -DAPP_PATH="${{ github.workspace }}/apps/$APP_FILE"

      - name: Upload Phase 1 Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase1-report
          path: |
            qa-automation/target/surefire-reports/
            qa-automation/reports/
            qa-automation/screenshots/
          retention-days: 7

      - name: Upload Appium Log (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: appium-log-phase1-${{ github.run_number }}
          path: appium.log
          retention-days: 7

      - name: Shutdown Simulator
        if: always()
        run: xcrun simctl shutdown "${{ env.SIMULATOR_UDID }}" 2>/dev/null || true

  # ============================================================
  # JOB 4: Asset Phase 2 Tests (159 tests, ~2.5 hr)
  # ============================================================
  phase2-tests:
    needs: [build-app]
    if: github.event.inputs.job_selection == 'all' || github.event.inputs.job_selection == 'phase2-only'
    runs-on: macos-15
    timeout-minutes: 420

    steps:
      - name: Download Built App
        uses: actions/download-artifact@v4
        with:
          name: built-app
          path: apps/

      - name: Download QA Automation Code
        uses: actions/download-artifact@v4
        with:
          name: qa-code
          path: qa-automation/

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Appium
        run: |
          npm install -g appium@latest 2>&1 | tail -1
          appium driver install xcuitest 2>&1 | tail -1
          echo "✅ Appium $(appium --version)"

      - name: Boot Simulator
        run: |
          UDID=""
          for VERSION in 18.5 18.4 18.3 18.2 18.1 18.0; do
            for MODEL in "iPhone 16 Pro" "iPhone 15 Pro" "iPhone"; do
              UDID=$(xcrun simctl list devices available | grep -A 100 "iOS $VERSION" | grep "$MODEL" | head -1 | awk -F '[()]' '{print $2}')
              [ -n "$UDID" ] && break 2
            done
          done
          [ -z "$UDID" ] && echo "❌ No simulator found!" && exit 1

          DEVICE_NAME=$(xcrun simctl list devices available | grep "$UDID" | sed 's/(.*//' | xargs)
          PLATFORM_VERSION=$(xcrun simctl list devices available | grep -B 20 "$UDID" | grep "iOS" | tail -1 | sed 's/-- iOS //' | sed 's/ --.*//')
          [ -z "$PLATFORM_VERSION" ] && PLATFORM_VERSION="18.5"

          echo "DEVICE_NAME=$DEVICE_NAME" >> $GITHUB_ENV
          echo "SIMULATOR_UDID=$UDID" >> $GITHUB_ENV
          echo "PLATFORM_VERSION=$PLATFORM_VERSION" >> $GITHUB_ENV
          echo "📱 $DEVICE_NAME | iOS $PLATFORM_VERSION | $UDID"

          xcrun simctl shutdown "$UDID" 2>/dev/null || true
          sleep 3; xcrun simctl boot "$UDID" 2>/dev/null || true

          TIMEOUT=120; ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            if xcrun simctl list devices | grep "$UDID" | grep -q "(Booted)"; then
              echo "✅ Booted after ${ELAPSED}s"; break
            fi
            sleep 5; ELAPSED=$((ELAPSED + 5))
          done
          if [ $ELAPSED -ge $TIMEOUT ]; then echo "❌ Boot timeout"; exit 1; fi
          sleep 45; echo "✅ Simulator ready"

      - name: Install App on Simulator
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          xcrun simctl install "${{ env.SIMULATOR_UDID }}" "apps/$APP_FILE"

      - name: Pre-build WebDriverAgent
        run: |
          WDA_PATH=$(find ~/.appium -name "WebDriverAgent.xcodeproj" -type f 2>/dev/null | head -1 | xargs dirname)
          if [ -n "$WDA_PATH" ]; then
            cd "$WDA_PATH"
            xcodebuild build-for-testing -project WebDriverAgent.xcodeproj -scheme WebDriverAgentRunner \
              -destination "platform=iOS Simulator,id=${{ env.SIMULATOR_UDID }}" \
              -derivedDataPath /tmp/wda-build -quiet 2>&1 | tail -3
            echo "✅ WDA pre-built"
          fi

      - name: Start Appium Server
        run: |
          appium --relaxed-security --log-level error > appium.log 2>&1 &
          for i in {1..60}; do
            if curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4723/status 2>/dev/null | grep -q "200"; then
              echo "✅ Appium ready"; break
            fi
            sleep 2
          done
          sleep 10

      - name: Warm-up WDA Session
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          FULL_APP_PATH="${{ github.workspace }}/apps/$APP_FILE"

          for attempt in 1 2 3; do
            SESSION_RESPONSE=$(curl -s -X POST http://127.0.0.1:4723/session \
              -H "Content-Type: application/json" \
              -d '{"capabilities":{"alwaysMatch":{"platformName":"iOS","appium:automationName":"XCUITest","appium:deviceName":"'"${{ env.DEVICE_NAME }}"'","appium:udid":"'"${{ env.SIMULATOR_UDID }}"'","appium:platformVersion":"'"${{ env.PLATFORM_VERSION }}"'","appium:app":"'"$FULL_APP_PATH"'","appium:wdaLaunchTimeout":600000,"appium:wdaConnectionTimeout":300000,"appium:useNewWDA":false,"appium:noReset":false}}}' \
              --max-time 600 2>&1) || true
            SESSION_ID=$(echo "$SESSION_RESPONSE" | grep -o '"sessionId":"[^"]*"' | cut -d'"' -f4 || true)
            if [ -n "$SESSION_ID" ]; then
              echo "✅ WDA warm-up OK"; sleep 10
              curl -s -X DELETE "http://127.0.0.1:4723/session/$SESSION_ID" || true
              sleep 5; break
            else
              [ $attempt -lt 3 ] && sleep 30
            fi
          done

      - name: Run Phase 2 Tests
        continue-on-error: true
        run: |
          cd qa-automation
          APP_FILE=$(ls ../apps/ | grep ".app" | head -1)
          mvn test -B -q \
            -DsuiteXmlFile=src/test/resources/parallel/testng-phase2.xml \
            -DDEVICE_NAME="${{ env.DEVICE_NAME }}" \
            -DPLATFORM_VERSION="${{ env.PLATFORM_VERSION }}" \
            -DSIMULATOR_UDID="${{ env.SIMULATOR_UDID }}" \
            -DAPP_PATH="${{ github.workspace }}/apps/$APP_FILE"

      - name: Upload Phase 2 Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase2-report
          path: |
            qa-automation/target/surefire-reports/
            qa-automation/reports/
            qa-automation/screenshots/
          retention-days: 7

      - name: Upload Appium Log (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: appium-log-phase2-${{ github.run_number }}
          path: appium.log
          retention-days: 7

      - name: Shutdown Simulator
        if: always()
        run: xcrun simctl shutdown "${{ env.SIMULATOR_UDID }}" 2>/dev/null || true

  # ============================================================
  # JOB 5: Asset Phase 3 Tests (169 tests, ~2.5 hr)
  # ============================================================
  phase3-tests:
    needs: [build-app]
    if: github.event.inputs.job_selection == 'all' || github.event.inputs.job_selection == 'phase3-only'
    runs-on: macos-15
    timeout-minutes: 420

    steps:
      - name: Download Built App
        uses: actions/download-artifact@v4
        with:
          name: built-app
          path: apps/

      - name: Download QA Automation Code
        uses: actions/download-artifact@v4
        with:
          name: qa-code
          path: qa-automation/

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Appium
        run: |
          npm install -g appium@latest 2>&1 | tail -1
          appium driver install xcuitest 2>&1 | tail -1
          echo "✅ Appium $(appium --version)"

      - name: Boot Simulator
        run: |
          UDID=""
          for VERSION in 18.5 18.4 18.3 18.2 18.1 18.0; do
            for MODEL in "iPhone 16 Pro" "iPhone 15 Pro" "iPhone"; do
              UDID=$(xcrun simctl list devices available | grep -A 100 "iOS $VERSION" | grep "$MODEL" | head -1 | awk -F '[()]' '{print $2}')
              [ -n "$UDID" ] && break 2
            done
          done
          [ -z "$UDID" ] && echo "❌ No simulator found!" && exit 1

          DEVICE_NAME=$(xcrun simctl list devices available | grep "$UDID" | sed 's/(.*//' | xargs)
          PLATFORM_VERSION=$(xcrun simctl list devices available | grep -B 20 "$UDID" | grep "iOS" | tail -1 | sed 's/-- iOS //' | sed 's/ --.*//')
          [ -z "$PLATFORM_VERSION" ] && PLATFORM_VERSION="18.5"

          echo "DEVICE_NAME=$DEVICE_NAME" >> $GITHUB_ENV
          echo "SIMULATOR_UDID=$UDID" >> $GITHUB_ENV
          echo "PLATFORM_VERSION=$PLATFORM_VERSION" >> $GITHUB_ENV

          xcrun simctl shutdown "$UDID" 2>/dev/null || true
          sleep 3; xcrun simctl boot "$UDID" 2>/dev/null || true

          TIMEOUT=120; ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            if xcrun simctl list devices | grep "$UDID" | grep -q "(Booted)"; then
              echo "✅ Booted after ${ELAPSED}s"; break
            fi
            sleep 5; ELAPSED=$((ELAPSED + 5))
          done
          if [ $ELAPSED -ge $TIMEOUT ]; then echo "❌ Boot timeout"; exit 1; fi
          sleep 45; echo "✅ Simulator ready"

      - name: Install App on Simulator
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          xcrun simctl install "${{ env.SIMULATOR_UDID }}" "apps/$APP_FILE"

      - name: Pre-build WebDriverAgent
        run: |
          WDA_PATH=$(find ~/.appium -name "WebDriverAgent.xcodeproj" -type f 2>/dev/null | head -1 | xargs dirname)
          if [ -n "$WDA_PATH" ]; then
            cd "$WDA_PATH"
            xcodebuild build-for-testing -project WebDriverAgent.xcodeproj -scheme WebDriverAgentRunner \
              -destination "platform=iOS Simulator,id=${{ env.SIMULATOR_UDID }}" \
              -derivedDataPath /tmp/wda-build -quiet 2>&1 | tail -3
            echo "✅ WDA pre-built"
          fi

      - name: Start Appium Server
        run: |
          appium --relaxed-security --log-level error > appium.log 2>&1 &
          for i in {1..60}; do
            if curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4723/status 2>/dev/null | grep -q "200"; then
              echo "✅ Appium ready"; break
            fi
            sleep 2
          done
          sleep 10

      - name: Warm-up WDA Session
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          FULL_APP_PATH="${{ github.workspace }}/apps/$APP_FILE"
          for attempt in 1 2 3; do
            SESSION_RESPONSE=$(curl -s -X POST http://127.0.0.1:4723/session \
              -H "Content-Type: application/json" \
              -d '{"capabilities":{"alwaysMatch":{"platformName":"iOS","appium:automationName":"XCUITest","appium:deviceName":"'"${{ env.DEVICE_NAME }}"'","appium:udid":"'"${{ env.SIMULATOR_UDID }}"'","appium:platformVersion":"'"${{ env.PLATFORM_VERSION }}"'","appium:app":"'"$FULL_APP_PATH"'","appium:wdaLaunchTimeout":600000,"appium:wdaConnectionTimeout":300000,"appium:useNewWDA":false,"appium:noReset":false}}}' \
              --max-time 600 2>&1) || true
            SESSION_ID=$(echo "$SESSION_RESPONSE" | grep -o '"sessionId":"[^"]*"' | cut -d'"' -f4 || true)
            if [ -n "$SESSION_ID" ]; then
              echo "✅ WDA warm-up OK"; sleep 10
              curl -s -X DELETE "http://127.0.0.1:4723/session/$SESSION_ID" || true; sleep 5; break
            else
              [ $attempt -lt 3 ] && sleep 30
            fi
          done

      - name: Run Phase 3 Tests
        continue-on-error: true
        run: |
          cd qa-automation
          APP_FILE=$(ls ../apps/ | grep ".app" | head -1)
          mvn test -B -q \
            -DsuiteXmlFile=src/test/resources/parallel/testng-phase3.xml \
            -DDEVICE_NAME="${{ env.DEVICE_NAME }}" \
            -DPLATFORM_VERSION="${{ env.PLATFORM_VERSION }}" \
            -DSIMULATOR_UDID="${{ env.SIMULATOR_UDID }}" \
            -DAPP_PATH="${{ github.workspace }}/apps/$APP_FILE"

      - name: Upload Phase 3 Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase3-report
          path: |
            qa-automation/target/surefire-reports/
            qa-automation/reports/
            qa-automation/screenshots/
          retention-days: 7

      - name: Upload Appium Log (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: appium-log-phase3-${{ github.run_number }}
          path: appium.log
          retention-days: 7

      - name: Shutdown Simulator
        if: always()
        run: xcrun simctl shutdown "${{ env.SIMULATOR_UDID }}" 2>/dev/null || true


  # ============================================================
  # JOB 6: Asset Phase 4 Tests (141 tests, ~2 hr)
  # ============================================================
  phase4-tests:
    needs: [build-app]
    if: github.event.inputs.job_selection == 'all' || github.event.inputs.job_selection == 'phase4-only'
    runs-on: macos-15
    timeout-minutes: 420

    steps:
      - name: Download Built App
        uses: actions/download-artifact@v4
        with:
          name: built-app
          path: apps/

      - name: Download QA Automation Code
        uses: actions/download-artifact@v4
        with:
          name: qa-code
          path: qa-automation/

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Appium
        run: |
          npm install -g appium@latest 2>&1 | tail -1
          appium driver install xcuitest 2>&1 | tail -1
          echo "✅ Appium $(appium --version)"

      - name: Boot Simulator
        run: |
          UDID=""
          for VERSION in 18.5 18.4 18.3 18.2 18.1 18.0; do
            for MODEL in "iPhone 16 Pro" "iPhone 15 Pro" "iPhone"; do
              UDID=$(xcrun simctl list devices available | grep -A 100 "iOS $VERSION" | grep "$MODEL" | head -1 | awk -F '[()]' '{print $2}')
              [ -n "$UDID" ] && break 2
            done
          done
          [ -z "$UDID" ] && echo "❌ No simulator found!" && exit 1

          DEVICE_NAME=$(xcrun simctl list devices available | grep "$UDID" | sed 's/(.*//' | xargs)
          PLATFORM_VERSION=$(xcrun simctl list devices available | grep -B 20 "$UDID" | grep "iOS" | tail -1 | sed 's/-- iOS //' | sed 's/ --.*//')
          [ -z "$PLATFORM_VERSION" ] && PLATFORM_VERSION="18.5"

          echo "DEVICE_NAME=$DEVICE_NAME" >> $GITHUB_ENV
          echo "SIMULATOR_UDID=$UDID" >> $GITHUB_ENV
          echo "PLATFORM_VERSION=$PLATFORM_VERSION" >> $GITHUB_ENV

          xcrun simctl shutdown "$UDID" 2>/dev/null || true
          sleep 3; xcrun simctl boot "$UDID" 2>/dev/null || true

          TIMEOUT=120; ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            if xcrun simctl list devices | grep "$UDID" | grep -q "(Booted)"; then
              echo "✅ Booted after ${ELAPSED}s"; break
            fi
            sleep 5; ELAPSED=$((ELAPSED + 5))
          done
          if [ $ELAPSED -ge $TIMEOUT ]; then echo "❌ Boot timeout"; exit 1; fi
          sleep 45; echo "✅ Simulator ready"

      - name: Install App on Simulator
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          xcrun simctl install "${{ env.SIMULATOR_UDID }}" "apps/$APP_FILE"

      - name: Pre-build WebDriverAgent
        run: |
          WDA_PATH=$(find ~/.appium -name "WebDriverAgent.xcodeproj" -type f 2>/dev/null | head -1 | xargs dirname)
          if [ -n "$WDA_PATH" ]; then
            cd "$WDA_PATH"
            xcodebuild build-for-testing -project WebDriverAgent.xcodeproj -scheme WebDriverAgentRunner \
              -destination "platform=iOS Simulator,id=${{ env.SIMULATOR_UDID }}" \
              -derivedDataPath /tmp/wda-build -quiet 2>&1 | tail -3
            echo "✅ WDA pre-built"
          fi

      - name: Start Appium Server
        run: |
          appium --relaxed-security --log-level error > appium.log 2>&1 &
          for i in {1..60}; do
            if curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4723/status 2>/dev/null | grep -q "200"; then
              echo "✅ Appium ready"; break
            fi
            sleep 2
          done
          sleep 10

      - name: Warm-up WDA Session
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          FULL_APP_PATH="${{ github.workspace }}/apps/$APP_FILE"
          for attempt in 1 2 3; do
            SESSION_RESPONSE=$(curl -s -X POST http://127.0.0.1:4723/session \
              -H "Content-Type: application/json" \
              -d '{"capabilities":{"alwaysMatch":{"platformName":"iOS","appium:automationName":"XCUITest","appium:deviceName":"'"${{ env.DEVICE_NAME }}"'","appium:udid":"'"${{ env.SIMULATOR_UDID }}"'","appium:platformVersion":"'"${{ env.PLATFORM_VERSION }}"'","appium:app":"'"$FULL_APP_PATH"'","appium:wdaLaunchTimeout":600000,"appium:wdaConnectionTimeout":300000,"appium:useNewWDA":false,"appium:noReset":false}}}' \
              --max-time 600 2>&1) || true
            SESSION_ID=$(echo "$SESSION_RESPONSE" | grep -o '"sessionId":"[^"]*"' | cut -d'"' -f4 || true)
            if [ -n "$SESSION_ID" ]; then
              echo "✅ WDA warm-up OK"; sleep 10
              curl -s -X DELETE "http://127.0.0.1:4723/session/$SESSION_ID" || true; sleep 5; break
            else
              [ $attempt -lt 3 ] && sleep 30
            fi
          done

      - name: Run Phase 4 Tests
        continue-on-error: true
        run: |
          cd qa-automation
          APP_FILE=$(ls ../apps/ | grep ".app" | head -1)
          mvn test -B -q \
            -DsuiteXmlFile=src/test/resources/parallel/testng-phase4.xml \
            -DDEVICE_NAME="${{ env.DEVICE_NAME }}" \
            -DPLATFORM_VERSION="${{ env.PLATFORM_VERSION }}" \
            -DSIMULATOR_UDID="${{ env.SIMULATOR_UDID }}" \
            -DAPP_PATH="${{ github.workspace }}/apps/$APP_FILE"

      - name: Upload Phase 4 Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase4-report
          path: |
            qa-automation/target/surefire-reports/
            qa-automation/reports/
            qa-automation/screenshots/
          retention-days: 7

      - name: Upload Appium Log (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: appium-log-phase4-${{ github.run_number }}
          path: appium.log
          retention-days: 7

      - name: Shutdown Simulator
        if: always()
        run: xcrun simctl shutdown "${{ env.SIMULATOR_UDID }}" 2>/dev/null || true

  # ============================================================
  # JOB 7: Asset Phase 5 Tests (45 tests, ~40 min)
  # ============================================================
  phase5-tests:
    needs: [build-app]
    if: github.event.inputs.job_selection == 'all' || github.event.inputs.job_selection == 'phase5-only'
    runs-on: macos-15
    timeout-minutes: 420

    steps:
      - name: Download Built App
        uses: actions/download-artifact@v4
        with:
          name: built-app
          path: apps/

      - name: Download QA Automation Code
        uses: actions/download-artifact@v4
        with:
          name: qa-code
          path: qa-automation/

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Appium
        run: |
          npm install -g appium@latest 2>&1 | tail -1
          appium driver install xcuitest 2>&1 | tail -1
          echo "✅ Appium $(appium --version)"

      - name: Boot Simulator
        run: |
          UDID=""
          for VERSION in 18.5 18.4 18.3 18.2 18.1 18.0; do
            for MODEL in "iPhone 16 Pro" "iPhone 15 Pro" "iPhone"; do
              UDID=$(xcrun simctl list devices available | grep -A 100 "iOS $VERSION" | grep "$MODEL" | head -1 | awk -F '[()]' '{print $2}')
              [ -n "$UDID" ] && break 2
            done
          done
          [ -z "$UDID" ] && echo "❌ No simulator found!" && exit 1

          DEVICE_NAME=$(xcrun simctl list devices available | grep "$UDID" | sed 's/(.*//' | xargs)
          PLATFORM_VERSION=$(xcrun simctl list devices available | grep -B 20 "$UDID" | grep "iOS" | tail -1 | sed 's/-- iOS //' | sed 's/ --.*//')
          [ -z "$PLATFORM_VERSION" ] && PLATFORM_VERSION="18.5"

          echo "DEVICE_NAME=$DEVICE_NAME" >> $GITHUB_ENV
          echo "SIMULATOR_UDID=$UDID" >> $GITHUB_ENV
          echo "PLATFORM_VERSION=$PLATFORM_VERSION" >> $GITHUB_ENV

          xcrun simctl shutdown "$UDID" 2>/dev/null || true
          sleep 3; xcrun simctl boot "$UDID" 2>/dev/null || true

          TIMEOUT=120; ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            if xcrun simctl list devices | grep "$UDID" | grep -q "(Booted)"; then
              echo "✅ Booted after ${ELAPSED}s"; break
            fi
            sleep 5; ELAPSED=$((ELAPSED + 5))
          done
          if [ $ELAPSED -ge $TIMEOUT ]; then echo "❌ Boot timeout"; exit 1; fi
          sleep 45; echo "✅ Simulator ready"

      - name: Install App on Simulator
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          xcrun simctl install "${{ env.SIMULATOR_UDID }}" "apps/$APP_FILE"

      - name: Pre-build WebDriverAgent
        run: |
          WDA_PATH=$(find ~/.appium -name "WebDriverAgent.xcodeproj" -type f 2>/dev/null | head -1 | xargs dirname)
          if [ -n "$WDA_PATH" ]; then
            cd "$WDA_PATH"
            xcodebuild build-for-testing -project WebDriverAgent.xcodeproj -scheme WebDriverAgentRunner \
              -destination "platform=iOS Simulator,id=${{ env.SIMULATOR_UDID }}" \
              -derivedDataPath /tmp/wda-build -quiet 2>&1 | tail -3
            echo "✅ WDA pre-built"
          fi

      - name: Start Appium Server
        run: |
          appium --relaxed-security --log-level error > appium.log 2>&1 &
          for i in {1..60}; do
            if curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4723/status 2>/dev/null | grep -q "200"; then
              echo "✅ Appium ready"; break
            fi
            sleep 2
          done
          sleep 10

      - name: Warm-up WDA Session
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          FULL_APP_PATH="${{ github.workspace }}/apps/$APP_FILE"
          for attempt in 1 2 3; do
            SESSION_RESPONSE=$(curl -s -X POST http://127.0.0.1:4723/session \
              -H "Content-Type: application/json" \
              -d '{"capabilities":{"alwaysMatch":{"platformName":"iOS","appium:automationName":"XCUITest","appium:deviceName":"'"${{ env.DEVICE_NAME }}"'","appium:udid":"'"${{ env.SIMULATOR_UDID }}"'","appium:platformVersion":"'"${{ env.PLATFORM_VERSION }}"'","appium:app":"'"$FULL_APP_PATH"'","appium:wdaLaunchTimeout":600000,"appium:wdaConnectionTimeout":300000,"appium:useNewWDA":false,"appium:noReset":false}}}' \
              --max-time 600 2>&1) || true
            SESSION_ID=$(echo "$SESSION_RESPONSE" | grep -o '"sessionId":"[^"]*"' | cut -d'"' -f4 || true)
            if [ -n "$SESSION_ID" ]; then
              echo "✅ WDA warm-up OK"; sleep 10
              curl -s -X DELETE "http://127.0.0.1:4723/session/$SESSION_ID" || true; sleep 5; break
            else
              [ $attempt -lt 3 ] && sleep 30
            fi
          done

      - name: Run Phase 5 Tests
        continue-on-error: true
        run: |
          cd qa-automation
          APP_FILE=$(ls ../apps/ | grep ".app" | head -1)
          mvn test -B -q \
            -DsuiteXmlFile=src/test/resources/parallel/testng-phase5.xml \
            -DDEVICE_NAME="${{ env.DEVICE_NAME }}" \
            -DPLATFORM_VERSION="${{ env.PLATFORM_VERSION }}" \
            -DSIMULATOR_UDID="${{ env.SIMULATOR_UDID }}" \
            -DAPP_PATH="${{ github.workspace }}/apps/$APP_FILE"

      - name: Upload Phase 5 Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase5-report
          path: |
            qa-automation/target/surefire-reports/
            qa-automation/reports/
            qa-automation/screenshots/
          retention-days: 7

      - name: Upload Appium Log (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: appium-log-phase5-${{ github.run_number }}
          path: appium.log
          retention-days: 7

      - name: Shutdown Simulator
        if: always()
        run: xcrun simctl shutdown "${{ env.SIMULATOR_UDID }}" 2>/dev/null || true

  # ============================================================
  # JOB 8: Location Tests (82 tests, ~1.5 hr)
  # ============================================================
  location-tests:
    needs: [build-app]
    if: github.event.inputs.job_selection == 'all' || github.event.inputs.job_selection == 'location-only'
    runs-on: macos-15
    timeout-minutes: 420

    steps:
      - name: Download Built App
        uses: actions/download-artifact@v4
        with:
          name: built-app
          path: apps/

      - name: Download QA Automation Code
        uses: actions/download-artifact@v4
        with:
          name: qa-code
          path: qa-automation/

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Appium
        run: |
          npm install -g appium@latest 2>&1 | tail -1
          appium driver install xcuitest 2>&1 | tail -1
          echo "✅ Appium $(appium --version)"

      - name: Boot Simulator
        run: |
          UDID=""
          for VERSION in 18.5 18.4 18.3 18.2 18.1 18.0; do
            for MODEL in "iPhone 16 Pro" "iPhone 15 Pro" "iPhone"; do
              UDID=$(xcrun simctl list devices available | grep -A 100 "iOS $VERSION" | grep "$MODEL" | head -1 | awk -F '[()]' '{print $2}')
              [ -n "$UDID" ] && break 2
            done
          done
          [ -z "$UDID" ] && echo "❌ No simulator found!" && exit 1

          DEVICE_NAME=$(xcrun simctl list devices available | grep "$UDID" | sed 's/(.*//' | xargs)
          PLATFORM_VERSION=$(xcrun simctl list devices available | grep -B 20 "$UDID" | grep "iOS" | tail -1 | sed 's/-- iOS //' | sed 's/ --.*//')
          [ -z "$PLATFORM_VERSION" ] && PLATFORM_VERSION="18.5"

          echo "DEVICE_NAME=$DEVICE_NAME" >> $GITHUB_ENV
          echo "SIMULATOR_UDID=$UDID" >> $GITHUB_ENV
          echo "PLATFORM_VERSION=$PLATFORM_VERSION" >> $GITHUB_ENV

          xcrun simctl shutdown "$UDID" 2>/dev/null || true
          sleep 3; xcrun simctl boot "$UDID" 2>/dev/null || true

          TIMEOUT=120; ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            if xcrun simctl list devices | grep "$UDID" | grep -q "(Booted)"; then
              echo "✅ Booted after ${ELAPSED}s"; break
            fi
            sleep 5; ELAPSED=$((ELAPSED + 5))
          done
          if [ $ELAPSED -ge $TIMEOUT ]; then echo "❌ Boot timeout"; exit 1; fi
          sleep 45; echo "✅ Simulator ready"

      - name: Install App on Simulator
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          xcrun simctl install "${{ env.SIMULATOR_UDID }}" "apps/$APP_FILE"

      - name: Pre-build WebDriverAgent
        run: |
          WDA_PATH=$(find ~/.appium -name "WebDriverAgent.xcodeproj" -type f 2>/dev/null | head -1 | xargs dirname)
          if [ -n "$WDA_PATH" ]; then
            cd "$WDA_PATH"
            xcodebuild build-for-testing -project WebDriverAgent.xcodeproj -scheme WebDriverAgentRunner \
              -destination "platform=iOS Simulator,id=${{ env.SIMULATOR_UDID }}" \
              -derivedDataPath /tmp/wda-build -quiet 2>&1 | tail -3
            echo "✅ WDA pre-built"
          fi

      - name: Start Appium Server
        run: |
          appium --relaxed-security --log-level error > appium.log 2>&1 &
          for i in {1..60}; do
            if curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4723/status 2>/dev/null | grep -q "200"; then
              echo "✅ Appium ready"; break
            fi
            sleep 2
          done
          sleep 10

      - name: Warm-up WDA Session
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          FULL_APP_PATH="${{ github.workspace }}/apps/$APP_FILE"
          for attempt in 1 2 3; do
            SESSION_RESPONSE=$(curl -s -X POST http://127.0.0.1:4723/session \
              -H "Content-Type: application/json" \
              -d '{"capabilities":{"alwaysMatch":{"platformName":"iOS","appium:automationName":"XCUITest","appium:deviceName":"'"${{ env.DEVICE_NAME }}"'","appium:udid":"'"${{ env.SIMULATOR_UDID }}"'","appium:platformVersion":"'"${{ env.PLATFORM_VERSION }}"'","appium:app":"'"$FULL_APP_PATH"'","appium:wdaLaunchTimeout":600000,"appium:wdaConnectionTimeout":300000,"appium:useNewWDA":false,"appium:noReset":false}}}' \
              --max-time 600 2>&1) || true
            SESSION_ID=$(echo "$SESSION_RESPONSE" | grep -o '"sessionId":"[^"]*"' | cut -d'"' -f4 || true)
            if [ -n "$SESSION_ID" ]; then
              echo "✅ WDA warm-up OK"; sleep 10
              curl -s -X DELETE "http://127.0.0.1:4723/session/$SESSION_ID" || true; sleep 5; break
            else
              [ $attempt -lt 3 ] && sleep 30
            fi
          done

      - name: Run Location Tests
        continue-on-error: true
        run: |
          cd qa-automation
          APP_FILE=$(ls ../apps/ | grep ".app" | head -1)
          mvn test -B -q \
            -DsuiteXmlFile=src/test/resources/parallel/testng-location.xml \
            -DDEVICE_NAME="${{ env.DEVICE_NAME }}" \
            -DPLATFORM_VERSION="${{ env.PLATFORM_VERSION }}" \
            -DSIMULATOR_UDID="${{ env.SIMULATOR_UDID }}" \
            -DAPP_PATH="${{ github.workspace }}/apps/$APP_FILE"

      - name: Upload Location Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: location-report
          path: |
            qa-automation/target/surefire-reports/
            qa-automation/reports/
            qa-automation/screenshots/
          retention-days: 7

      - name: Upload Appium Log (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: appium-log-location-${{ github.run_number }}
          path: appium.log
          retention-days: 7

      - name: Shutdown Simulator
        if: always()
        run: xcrun simctl shutdown "${{ env.SIMULATOR_UDID }}" 2>/dev/null || true

  # ============================================================
  # JOB 9: Connections Tests (94 tests, ~2 hr)
  # ============================================================
  connections-tests:
    needs: [build-app]
    if: github.event.inputs.job_selection == 'all' || github.event.inputs.job_selection == 'connections-only'
    runs-on: macos-15
    timeout-minutes: 420

    steps:
      - name: Download Built App
        uses: actions/download-artifact@v4
        with:
          name: built-app
          path: apps/

      - name: Download QA Automation Code
        uses: actions/download-artifact@v4
        with:
          name: qa-code
          path: qa-automation/

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Appium
        run: |
          npm install -g appium@latest 2>&1 | tail -1
          appium driver install xcuitest 2>&1 | tail -1
          echo "✅ Appium $(appium --version)"

      - name: Boot Simulator
        run: |
          UDID=""
          for VERSION in 18.5 18.4 18.3 18.2 18.1 18.0; do
            for MODEL in "iPhone 16 Pro" "iPhone 15 Pro" "iPhone"; do
              UDID=$(xcrun simctl list devices available | grep -A 100 "iOS $VERSION" | grep "$MODEL" | head -1 | awk -F '[()]' '{print $2}')
              [ -n "$UDID" ] && break 2
            done
          done
          [ -z "$UDID" ] && echo "❌ No simulator found!" && exit 1

          DEVICE_NAME=$(xcrun simctl list devices available | grep "$UDID" | sed 's/(.*//' | xargs)
          PLATFORM_VERSION=$(xcrun simctl list devices available | grep -B 20 "$UDID" | grep "iOS" | tail -1 | sed 's/-- iOS //' | sed 's/ --.*//')
          [ -z "$PLATFORM_VERSION" ] && PLATFORM_VERSION="18.5"

          echo "DEVICE_NAME=$DEVICE_NAME" >> $GITHUB_ENV
          echo "SIMULATOR_UDID=$UDID" >> $GITHUB_ENV
          echo "PLATFORM_VERSION=$PLATFORM_VERSION" >> $GITHUB_ENV

          xcrun simctl shutdown "$UDID" 2>/dev/null || true
          sleep 3; xcrun simctl boot "$UDID" 2>/dev/null || true

          TIMEOUT=120; ELAPSED=0
          while [ $ELAPSED -lt $TIMEOUT ]; do
            if xcrun simctl list devices | grep "$UDID" | grep -q "(Booted)"; then
              echo "✅ Booted after ${ELAPSED}s"; break
            fi
            sleep 5; ELAPSED=$((ELAPSED + 5))
          done
          if [ $ELAPSED -ge $TIMEOUT ]; then echo "❌ Boot timeout"; exit 1; fi
          sleep 45; echo "✅ Simulator ready"

      - name: Install App on Simulator
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          xcrun simctl install "${{ env.SIMULATOR_UDID }}" "apps/$APP_FILE"

      - name: Pre-build WebDriverAgent
        run: |
          WDA_PATH=$(find ~/.appium -name "WebDriverAgent.xcodeproj" -type f 2>/dev/null | head -1 | xargs dirname)
          if [ -n "$WDA_PATH" ]; then
            cd "$WDA_PATH"
            xcodebuild build-for-testing -project WebDriverAgent.xcodeproj -scheme WebDriverAgentRunner \
              -destination "platform=iOS Simulator,id=${{ env.SIMULATOR_UDID }}" \
              -derivedDataPath /tmp/wda-build -quiet 2>&1 | tail -3
            echo "✅ WDA pre-built"
          fi

      - name: Start Appium Server
        run: |
          appium --relaxed-security --log-level error > appium.log 2>&1 &
          for i in {1..60}; do
            if curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4723/status 2>/dev/null | grep -q "200"; then
              echo "✅ Appium ready"; break
            fi
            sleep 2
          done
          sleep 10

      - name: Warm-up WDA Session
        run: |
          APP_FILE=$(ls apps/ | grep ".app" | head -1)
          FULL_APP_PATH="${{ github.workspace }}/apps/$APP_FILE"
          for attempt in 1 2 3; do
            SESSION_RESPONSE=$(curl -s -X POST http://127.0.0.1:4723/session \
              -H "Content-Type: application/json" \
              -d '{"capabilities":{"alwaysMatch":{"platformName":"iOS","appium:automationName":"XCUITest","appium:deviceName":"'"${{ env.DEVICE_NAME }}"'","appium:udid":"'"${{ env.SIMULATOR_UDID }}"'","appium:platformVersion":"'"${{ env.PLATFORM_VERSION }}"'","appium:app":"'"$FULL_APP_PATH"'","appium:wdaLaunchTimeout":600000,"appium:wdaConnectionTimeout":300000,"appium:useNewWDA":false,"appium:noReset":false}}}' \
              --max-time 600 2>&1) || true
            SESSION_ID=$(echo "$SESSION_RESPONSE" | grep -o '"sessionId":"[^"]*"' | cut -d'"' -f4 || true)
            if [ -n "$SESSION_ID" ]; then
              echo "✅ WDA warm-up OK"; sleep 10
              curl -s -X DELETE "http://127.0.0.1:4723/session/$SESSION_ID" || true; sleep 5; break
            else
              [ $attempt -lt 3 ] && sleep 30
            fi
          done

      - name: Run Connections Tests
        continue-on-error: true
        run: |
          cd qa-automation
          APP_FILE=$(ls ../apps/ | grep ".app" | head -1)
          mvn test -B -q \
            -DsuiteXmlFile=src/test/resources/parallel/testng-connections.xml \
            -DDEVICE_NAME="${{ env.DEVICE_NAME }}" \
            -DPLATFORM_VERSION="${{ env.PLATFORM_VERSION }}" \
            -DSIMULATOR_UDID="${{ env.SIMULATOR_UDID }}" \
            -DAPP_PATH="${{ github.workspace }}/apps/$APP_FILE"

      - name: Upload Connections Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: connections-report
          path: |
            qa-automation/target/surefire-reports/
            qa-automation/reports/
            qa-automation/screenshots/
          retention-days: 7

      - name: Upload Appium Log (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: appium-log-connections-${{ github.run_number }}
          path: appium.log
          retention-days: 7

      - name: Shutdown Simulator
        if: always()
        run: xcrun simctl shutdown "${{ env.SIMULATOR_UDID }}" 2>/dev/null || true

  # ============================================================
  # JOB 10: Summary & Email
  # ============================================================
  summary:
    needs: [auth-tests, site-tests, phase1-tests, phase2-tests, phase3-tests, phase4-tests, phase5-tests, location-tests, connections-tests]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Download All Reports
        uses: actions/download-artifact@v4
        with:
          path: all-reports
        continue-on-error: true

      - name: Prepare Summary
        id: summary
        run: |
          AUTH="${{ needs.auth-tests.result }}"
          SITE="${{ needs.site-tests.result }}"
          P1="${{ needs.phase1-tests.result }}"
          P2="${{ needs.phase2-tests.result }}"
          P3="${{ needs.phase3-tests.result }}"
          P4="${{ needs.phase4-tests.result }}"
          P5="${{ needs.phase5-tests.result }}"
          LOC="${{ needs.location-tests.result }}"
          CONN="${{ needs.connections-tests.result }}"

          SUCCESS=0
          [ "$AUTH" = "success" ] && SUCCESS=$((SUCCESS+1))
          [ "$SITE" = "success" ] && SUCCESS=$((SUCCESS+1))
          [ "$P1" = "success" ] && SUCCESS=$((SUCCESS+1))
          [ "$P2" = "success" ] && SUCCESS=$((SUCCESS+1))
          [ "$P3" = "success" ] && SUCCESS=$((SUCCESS+1))
          [ "$P4" = "success" ] && SUCCESS=$((SUCCESS+1))
          [ "$P5" = "success" ] && SUCCESS=$((SUCCESS+1))
          [ "$LOC" = "success" ] && SUCCESS=$((SUCCESS+1))
          [ "$CONN" = "success" ] && SUCCESS=$((SUCCESS+1))

          FAILED=$((9-SUCCESS))
          echo "SUCCESS=$SUCCESS" >> $GITHUB_OUTPUT
          echo "FAILED=$FAILED" >> $GITHUB_OUTPUT

          if [ $SUCCESS -eq 9 ]; then
            STATUS="✅ ALL PASSED"
          elif [ $SUCCESS -ge 7 ]; then
            STATUS="⚠️ MOSTLY PASSED"
          else
            STATUS="❌ FAILED"
          fi
          echo "STATUS=$STATUS" >> $GITHUB_OUTPUT

          SAFE_BRANCH=$(echo "${{ github.event.inputs.branch }}" | tr '/' '-')

          cat > email_body.html << EOF
          <!DOCTYPE html>
          <html>
          <body style="font-family: Arial, sans-serif; padding: 20px; max-width: 800px; margin: 0 auto;">
            <h1 style="color: #333;">🧪 iOS Full Parallel Suite Results (Developer Repo)</h1>
            <p><b>Branch:</b> ${{ github.event.inputs.branch }}</p>
            <p><b>Jobs Selected:</b> ${{ github.event.inputs.job_selection }}</p>
            <p><b>Triggered by:</b> ${{ github.actor }}</p>
            <p><b>Run:</b> <a href="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}">View Full Details</a></p>
            <hr>

            <h2>Results: ${SUCCESS}/9 Jobs Passed — ${STATUS}</h2>

            <table border="1" cellpadding="10" style="border-collapse: collapse; width: 100%;">
              <tr style="background-color: #4a90d9; color: white;">
                <th>Job</th><th>Tests</th><th>Status</th>
              </tr>
              <tr><td>Auth Tests</td><td>38</td><td>${AUTH}</td></tr>
              <tr><td>Site Tests</td><td>52</td><td>${SITE}</td></tr>
              <tr><td>Phase 1</td><td>139</td><td>${P1}</td></tr>
              <tr><td>Phase 2</td><td>159</td><td>${P2}</td></tr>
              <tr><td>Phase 3</td><td>169</td><td>${P3}</td></tr>
              <tr><td>Phase 4</td><td>141</td><td>${P4}</td></tr>
              <tr><td>Phase 5</td><td>45</td><td>${P5}</td></tr>
              <tr><td>Location</td><td>82</td><td>${LOC}</td></tr>
              <tr><td>Connections</td><td>94</td><td>${CONN}</td></tr>
            </table>

            <hr>
            <p><b>Total Tests:</b> ~921</p>
            <p><b>Jobs Passed:</b> ${SUCCESS} | <b>Failed:</b> ${FAILED}</p>
            <p style="margin-top: 20px; color: #666; font-size: 12px;">
              Automated report from eGalvanic iOS Automation Suite (Developer Repo).
            </p>
          </body>
          </html>
          EOF

      - name: Send Email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "[${{ steps.summary.outputs.STATUS }}] iOS Parallel Suite (${{ github.event.inputs.branch }}) - ${{ steps.summary.outputs.SUCCESS }}/9 Passed"
          html_body: file://email_body.html
          to: ${{ secrets.EMAIL_TO }}
          from: "iOS Automation <${{ secrets.EMAIL_USERNAME }}>"
        continue-on-error: true

      - name: Print Summary
        run: |
          echo ""
          echo "╔══════════════════════════════════════════════════════════════════╗"
          echo "║       DEVELOPER REPO - FULL PARALLEL SUITE COMPLETE              ║"
          echo "╚══════════════════════════════════════════════════════════════════╝"
          echo ""
          echo "  🌿 Branch:      ${{ github.event.inputs.branch }}"
          echo "  🎯 Selection:   ${{ github.event.inputs.job_selection }}"
          echo "  📊 Results:     ${{ steps.summary.outputs.SUCCESS }}/9 Jobs Passed"
          echo ""
          echo "  Auth (38):        ${{ needs.auth-tests.result }}"
          echo "  Site (52):        ${{ needs.site-tests.result }}"
          echo "  Phase 1 (139):    ${{ needs.phase1-tests.result }}"
          echo "  Phase 2 (159):    ${{ needs.phase2-tests.result }}"
          echo "  Phase 3 (169):    ${{ needs.phase3-tests.result }}"
          echo "  Phase 4 (141):    ${{ needs.phase4-tests.result }}"
          echo "  Phase 5 (45):     ${{ needs.phase5-tests.result }}"
          echo "  Location (82):    ${{ needs.location-tests.result }}"
          echo "  Connections (94): ${{ needs.connections-tests.result }}"
          echo ""
          echo "  📧 Email: sent to configured recipients"
          echo "  🔗 Details: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
