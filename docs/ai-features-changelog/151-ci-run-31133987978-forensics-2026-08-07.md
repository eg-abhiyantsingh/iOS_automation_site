# 151 — CI run 31133987978 forensics: 21 fails → 5 root causes, all fixed (2026-08-07)

## Context
First CI parallel run with the full fix stack (walker 81d2dc9 + delete drift
f83f130). Result: 3/9 worktype slices green (createpicker, catalog, createform
— first two were red the day before), 6 red with 21 unique failing tests.
Massive step forward from run 31081238514 (mass 6-minute kills, cancelled
jobs); every remaining fail was classified with log evidence:

## Root causes + fixes
1. **cross/NEG_02 (2s)** — junk-row DELETE got HTTP 200 with an **HTML body**
   (gateway page; request never reached the API). Body-shape validation
   correctly refused it. Fix: `TestDataApi.deleteWorkOrder` retries once
   (1.5s backoff); NEG_02 falls back to a name-absence probe — the contract
   is row absence, not response cosmetics. Local: PASS 7s.
2. **BEH_006/036/040 + DET_033/204 (~80s)** — "Fixture row must open
   (verified nav)": row-tap nav flake on 18.5. The entry-nav retry existed
   for dashboard→list but NOT the row tap. Fix:
   `WorkTypeBaseTest.openFixtureOrSkip` late-nav check + one re-scroll/re-open
   attempt. Local: BEH_006 PASS 1m06s.
3. **E2E_032 + 19 cascade skips** — pre-test session guard was one-shot
   (`menuOpened=false` → gave up → stale session poisoned every "no active
   session" assert). Fix: 3-attempt guard (re-anchor → app relaunch →
   final try) in `endActiveSessionIfAny`.
4. **forms 032–040, 045 (6m kills)** — NOT locators: FORM_031 PASSED at
   5m45s vs the 360s cap; 18.5 CI sims run ~2× local latency, so healthy
   tests scrape the ceiling. 032 wedged WDA mid-query (giant-DOM family),
   died; 033–040 burned 6m each in dead-session rebuild thrash; 041 rebuilt
   clean. The walker itself worked on CI (fast pass logged one line).
5. **list 201/207/211/212 (6m kills)** — full-list census sweeps are
   O(list length) at 2× latency; can't fit 360s.

Fix for 4+5: per-slice budget in ios-tests-parallel.yml — forms and list run
with `-DTEST_TIMEOUT_SECONDS=540`; other slices stay at 360.

## Fresh dispatch
run_worktype re-dispatched on this tree for the next verdict cycle.
