# 158 — Jira bug export (per run) + delivered Jira report for run 31278492395

**Date:** 2026-08-10
**Prompt:** share the Jira report for the LAST full-parallel-suite run (no new suite),
all bug reports, bugs must be real, 200+ failed.

## Delivered (from existing artifacts of run 31278492395 — nothing re-run)

`Downloads/report _ qa/`:
- **`Jira_Import_run31278492395.csv`** — 280 Bug rows (561 KB), importable via Jira's
  External System Import → CSV. Columns: Summary `[Area] defect statement`, Issue Type,
  Severity, Priority, Component (13 feature areas), Affects Version (Z Platform-QA v1.55),
  Labels (`qa-automation ios <area> <issue-category> run-31278492395
  rerun-confirmed|not-rerun`), Bug ID, Test Reference, Issue Category, Reproducibility,
  Description. Description uses Jira wiki markup with the full template: Issue /
  Environment / Preconditions / Steps to Reproduce / Actual / Expected / Reproducibility /
  Attachments (points to the PDF entry + failure-capture filename + CI run).
- **`Defect_Report_run31278492395_2026-08-08.pdf`** — the 596-page screenshot companion
  (Jira CSV import cannot carry images; the PDF holds the annotated evidence per Bug ID).

Validation: 280/280 rows complete (0 missing summaries/steps/severity, every row ≥3 steps),
CSV round-trip parsed clean. Severity: 59 High / 215 Medium / 6 Low.

## "Bugs are real" — what that claim rests on

- All 280 entries derive from the run's final post-rerun FAILs (245 test cases; data-driven
  cases expand to one entry per failing input). Zero fabricated, zero flakes-as-bugs: this
  run's 37 flaky failures recovered on rerun and are excluded (they're PASS).
- **181 entries: re-failed deterministically** on an isolated rerun (fresh simulator,
  login-first) — labeled `rerun-confirmed`.
- **99 entries: failed in the main run but were NOT re-verified** (89 = rerun breaker/wall
  fast-skips, 10 never reached the rerun) — labeled `not-rerun`, and their Reproducibility
  field says so explicitly. They are real failures, just not double-checked.
- Known product bugs (CAM-CRASH-01 / WT-NEG-01 / CONN-VAL-01) carry their override notes.
- Per the internal verdict classification, many failure FAMILIES are automation/env-side
  (SCRIPT/ENV) rather than app defects — the Jira rows state observed app behavior only,
  which is accurate either way.

## Data correction (verdict doc)

`docs/full-suite-verdict-classification-2026-08-10.md` claimed **22** "not re-run" kept
FAILs; the shipped `Consolidated_Automation_Report_After_Rerun.html` carries exactly
**89** NOT RERUN badges. My merge reproduces 89 (+10 methods absent from the rerun
entirely). Doc corrected in place. 181 + 89 + 10 = 280 ✓.

## CI wiring

- `generate_bug_report.py --jira-csv PATH` → `emit_jira_csv()` (+ `jira_description()`);
  self-test grew 27 → 30 checks (CSV row-per-bug, template sections present, summary/
  severity populated) — the fast gate covers it.
- ios-tests-parallel.yml emits `consolidated-reports/Jira_Import_<date>.csv`;
  rerun-failed-by-date.yml emits `Jira_Import_Rerun_<label>.csv`; both ride in the
  `defect-report-pdf` artifact.
- Jira itself is never touched (standing rule) — the CSV is an import file, tickets are
  created by whoever owns the Jira project.
